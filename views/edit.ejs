<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        ::-webkit-scrollbar{
            display: none;
        }
    </style>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-black p-10 text-white">
        <div class="flex justify-end w-full">
            <a href="/logout" class="w-fit h-fit pl-4 pr-4 pt-1 pb-2 bg-red-600 rounded-lg">Logout</a>
        </div>
        <div class="pt-5">
            <h1 class="text-xl font-semibold mb-4">Edit the post</h1>
            <form action="/update/<%= post._id %>" method="post" class="border-b border-b-white pb-5">
                <textarea placeholder="What's on your mind" class="p-3 outline-none text-zinc-400 w-1/3 resize-none bg-zinc-900 rounded-md block " name="content" ><%= post.content %></textarea>
                <input type="submit" class="w-36 block mt-3 px-3 py-2 bg-green-600 rounded-lg" value=" update post">
            </form>
        
        </div>
    </div>
</body>
</html>